# Sleep & Lifestyle Analytics Pipeline
# Install: pip install -r requirements.txt

# Kafka — kafka-python-ng is the maintained fork, works with Python 3.12+
# kafka-python 2.0.2 is broken on Python 3.12+ (missing kafka.vendor.six.moves)
kafka-python-ng

# Data generation
Faker==24.9.0

# PySpark — must match Kafka connector Scala build (2.12)
pyspark==3.5.3

# Parquet
pyarrow>=14.0.0

# Dashboard
dash==2.17.1
dash-bootstrap-components==1.6.0
plotly==5.22.0

# Data
pandas>=2.0.0
numpy>=1.26.0

# AWS (optional — only needed for S3 output)
boto3>=1.34.0
